<% include('layout', { subject }) %>

<h2>Complaint Status Update</h2>
<p>Hello,</p>
<p>The status of your complaint has been updated:</p>

<div style="background-color: #f9f9f9; padding: 15px; border-left: 4px solid #2196F3; margin: 15px 0;">
  <h3 style="margin-top: 0; color: #333;"><%= data.complaint.issueType %></h3>
  <p><strong>Ticket ID:</strong> <%= data.complaint.ticketId %></p>
  <p><strong>Status Changed:</strong> 
    <span style="text-transform: capitalize;"><%= data.oldStatus %></span> 
    → 
    <span style="text-transform: capitalize; font-weight: bold; color: #2196F3;"><%= data.complaint.status %></span>
  </p>
  <% if (data.complaint.statusUpdateNotes) { %>
    <p><strong>Update Notes:</strong></p>
    <p><%= data.complaint.statusUpdateNotes %></p>
  <% } %>
  <p><strong>Last Updated:</strong> <%= new Date(data.complaint.updatedAt).toLocaleString() %></p>
</div>

<p>You can view the latest updates and track the progress of your complaint by clicking the button below:</p>

<a href="<%= appUrl %>/complaints/<%= data.complaint._id %>" class="button" style="color: white;">View Complaint</a>

<p>If you have any questions or need further assistance, please don't hesitate to contact our support team.</p>

<p>Best regards,<br><%= appName %> Team</p>
